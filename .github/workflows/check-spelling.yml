name: "Check Spelling"

on: [push]

jobs:

# ---------------------------------------------------------------------------------------

  check-spelling:

# ---------------------------------------------------------------------------------------

    name: "Spell Checker"
    runs-on: ubuntu-20.04

    steps:
      - name: 🤘 Checkout Code
        uses: actions/checkout@v2 # https://github.com/actions/checkout

      - name: 🔽 Install Dependencies
        run: sudo npm install -g cspell

      - name: 🙏 Check Spelling using cspell
        run: |
          export CSPELL_OUTPUT=$(sudo cspell --no-summary "**.txt" "**.md" "docs/*.md" "src/**/*.h" "src/**/*.cpp" "tests/**/*.h" "tests/**/*.cpp")
          if [ -n "$CSPELL_OUTPUT" ]; then echo "$CSPELL_OUTPUT" && exit 1; fi;